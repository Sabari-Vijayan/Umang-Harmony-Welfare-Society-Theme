{{!< default}}

<section class="error-page section-padding">
    <div class="container text-center">
        <h1 class="error-title">{{@custom.error_title}}</h1>
        <p class="error-message">{{@custom.error_message}}</p>
        <div class="error-actions">
            <a href="{{@site.url}}" class="btn btn-primary">Back to Homepage</a>
            <a href="{{@site.url}}/contact-us/" class="btn btn-outline">Contact Support</a>
        </div>
    </div>
</section>
